#Import only if not previously imported
import cv2
img = cv2.imread("./image/test2.png",cv2.IMREAD_COLOR)     #(flag = 0 or 1 or -1)

#Import only if not previously imported
import cv2
cv2.imshow("AAAAAA",img)
cv2.waitKey(0)
cv2.destroyAllWindows()
